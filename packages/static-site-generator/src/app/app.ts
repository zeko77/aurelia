import { customElement } from "@aurelia/runtime";

// TODO enhance the template to include the layout template + menu
@customElement({ name: 'app', template: `<au-viewport default="home"></au-viewport>` })
export class App {

}
